# Allow all lifecycle scripts for packages that need them
# This is required for native modules like bcrypt, sharp, and Prisma
ignore-scripts=false

# Use legacy peer deps to avoid dependency conflicts
legacy-peer-deps=true

# Optional: Set registry if needed
# registry=https://registry.npmjs.org/

# Reduce log noise
loglevel=warn

# Enable progress bars
progress=true
